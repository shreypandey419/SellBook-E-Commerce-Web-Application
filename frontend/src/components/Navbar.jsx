import { useState } from "react";
import { Link, NavLink, useNavigate } from "react-router-dom";
import { FaBars, FaBookOpen, FaHeart, FaSearch, FaShoppingCart, FaTimes, FaUserCircle } from "react-icons/fa";
import { useCart } from "../CartContext/CartContext";
import { useWishlist } from "../WishlistContext/WishlistContext";
import { useAuthContext } from "../api/AuthContext";
import MiniCart from "./MiniCart";

function Navbar() {
  const [mobileOpen, setMobileOpen] = useState(false);
  const [cartOpen, setCartOpen] = useState(false);
  const [profileOpen, setProfileOpen] = useState(false);
  const [search, setSearch] = useState("");
  const navigate = useNavigate();
  const { cartCount } = useCart();
  const { wishlistCount } = useWishlist();
  const { user, logout } = useAuthContext();
  const links = [{ to: "/", label: "Home" }, { to: "/#latest", label: "Latest" }, { to: "/wishlist", label: "Wishlist" }];
  const submitSearch = (event) => { event.preventDefault(); navigate(`/?search=${encodeURIComponent(search)}`); setMobileOpen(false); };

  return <header className="sticky top-0 z-50 border-b border-slate-200/80 bg-white/90 backdrop-blur"><nav className="mx-auto flex max-w-7xl items-center gap-3 px-4 py-3 sm:px-6"><Link to="/" className="flex shrink-0 items-center gap-2 text-xl font-black tracking-tight text-slate-900"><span className="grid h-9 w-9 place-items-center rounded-xl bg-indigo-600 text-white"><FaBookOpen /></span><span className="hidden sm:inline">SellBook</span></Link><div className="hidden items-center gap-1 lg:flex">{links.map((link) => <NavLink key={link.label} to={link.to} className="rounded-lg px-3 py-2 text-sm font-semibold text-slate-600 transition hover:bg-indigo-50 hover:text-indigo-700">{link.label}</NavLink>)}</div><form onSubmit={submitSearch} className="relative ml-auto hidden max-w-sm flex-1 md:block"><FaSearch className="absolute left-3 top-3 text-slate-400" /><input value={search} onChange={(event) => setSearch(event.target.value)} aria-label="Search books" placeholder="Search books" className="w-full rounded-xl border border-slate-200 bg-slate-50 py-2 pl-9 pr-3 text-sm outline-none focus:border-indigo-500 focus:ring-2 focus:ring-indigo-100" /></form><div className="flex items-center gap-1"><Link to="/wishlist" aria-label="Wishlist" className="relative rounded-lg p-2 text-slate-600 hover:bg-slate-100"><FaHeart />{wishlistCount > 0 && <span className="absolute -right-1 -top-1 grid h-5 min-w-5 place-items-center rounded-full bg-rose-500 px-1 text-[10px] font-bold text-white">{wishlistCount}</span>}</Link><div className="relative" onMouseEnter={() => setCartOpen(true)} onMouseLeave={() => setCartOpen(false)}><Link to="/cart" aria-label="Cart" className="relative rounded-lg p-2 text-slate-600 hover:bg-slate-100"><FaShoppingCart />{cartCount > 0 && <span className="absolute -right-1 -top-1 grid h-5 min-w-5 place-items-center rounded-full bg-indigo-600 px-1 text-[10px] font-bold text-white">{cartCount}</span>}</Link>{cartOpen && <MiniCart closeCart={() => setCartOpen(false)} />}</div>{user ? <div className="relative"><button type="button" onClick={() => setProfileOpen(!profileOpen)} aria-label="Account menu" className="flex items-center gap-2 rounded-lg p-2 text-slate-700 hover:bg-slate-100"><FaUserCircle className="text-xl" /><span className="hidden max-w-24 truncate text-sm font-semibold sm:inline">{user.name}</span></button>{profileOpen && <div className="absolute right-0 mt-2 w-40 rounded-xl border border-slate-200 bg-white p-1 shadow-xl"><Link to="/profile" onClick={() => setProfileOpen(false)} className="block rounded-lg px-3 py-2 text-sm hover:bg-slate-50">Profile</Link><Link to="/orders" onClick={() => setProfileOpen(false)} className="block rounded-lg px-3 py-2 text-sm hover:bg-slate-50">My Orders</Link><button type="button" onClick={() => { logout(); setProfileOpen(false); navigate("/"); }} className="block w-full rounded-lg px-3 py-2 text-left text-sm text-rose-600 hover:bg-rose-50">Logout</button></div>}</div> : <Link to="/login" className="hidden rounded-lg bg-slate-900 px-3 py-2 text-sm font-bold text-white hover:bg-indigo-600 sm:block">Login</Link>}<button type="button" onClick={() => setMobileOpen(!mobileOpen)} className="rounded-lg p-2 lg:hidden" aria-label="Toggle menu">{mobileOpen ? <FaTimes /> : <FaBars />}</button></div></nav>{mobileOpen && <div className="border-t bg-white px-4 py-3 lg:hidden"><form onSubmit={submitSearch} className="relative"><FaSearch className="absolute left-3 top-3 text-slate-400" /><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search books" className="w-full rounded-xl border py-2 pl-9" /></form><div className="mt-3 grid grid-cols-3 gap-2">{links.map((link) => <NavLink key={link.label} to={link.to} onClick={() => setMobileOpen(false)} className="rounded-lg bg-slate-50 px-3 py-2 text-center text-sm font-medium">{link.label}</NavLink>)}</div></div>}</header>;
}

export default Navbar;
